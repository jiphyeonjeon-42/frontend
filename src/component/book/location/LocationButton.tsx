const LocationButton = () => {
  return (
    <div className="location_button_box">
      <button className="location_button" type="button">
        도서 위치
      </button>
    </div>
  );
};

export default LocationButton;
